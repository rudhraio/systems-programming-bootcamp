# Scripts

Utility scripts for checking the environment and running repeated commands.

Scripts should be small and readable. They are teaching aids, not hidden magic.

Run the environment check from the repository root:

```sh
sh scripts/check-env.sh
```
